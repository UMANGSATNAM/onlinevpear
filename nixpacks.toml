[phases.setup]
nixPkgs = ["nodejs_20", "bun"]
aptPkgs = ["openssl", "libssl-dev"]

[phases.install]
cmds = ["bun install --no-save"]

[phases.build]
cmds = ["bun run db:generate", "bun run build"]

[start]
cmd = "bun run db:push && bun x prisma db seed && bun run start"
